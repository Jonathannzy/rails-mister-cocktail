<div class="container">
  <% simple_form_for [@cocktail, @dose] do |d| %>
  <%= d.association :ingredient %>
  <%= d.input :description %>
  <%= d.submit :Submit, class: "btn btn-primary" %>
  <% end %>
</div>
